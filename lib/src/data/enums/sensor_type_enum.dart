/// TODO(freezed): convert [RuleType] to freezed object

/// Available sensor types in system.
enum SensorType {
  /// Used to display humidity on the sensor.
  humidity,

  /// Used to display temperature on the sensor.
  temperature,
}
