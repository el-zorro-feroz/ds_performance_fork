name: sensors_monitoring
description: sensors_monitoring project
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.3 <4.0.0"

dependencies:
  dartz: ^0.10.1
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  get_it: ^7.6.4
  injectable: ^2.3.1
  postgres: ^2.6.3

dev_dependencies:
  build_runner: ^2.4.6
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.4.0
  lints: ^2.1.1
  mockito: ^5.4.2

flutter:
  uses-material-design: true
